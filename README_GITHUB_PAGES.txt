GitHub Pages – Schnellstart
=============================

So machst du die App in 3–5 Minuten öffentlich erreichbar:

1) Neues Repository auf GitHub anlegen, z. B. **vokabeltrainer**.
2) Den gesamten Inhalt dieses ZIPs hochladen (inkl. **docs/** Ordner).
3) In den Repo-Einstellungen zu **Settings → Pages** gehen:
   - **Source:** `main` branch
   - **Folder:** `/docs`
   - **Save** klicken.
4) Nach 1–2 Minuten ist deine App online unter:
   `https://<dein-username>.github.io/<repo-name>/`

Tipp: Wenn du eine eigene Domain hast, lege in **docs/** eine Datei `CNAME` mit nur deiner Domain an (z. B. `app.deinedomain.ch`).

Lokal testen:
- Öffne `docs/index.html` direkt im Browser oder nutze VS Code + Live Server.

Viel Spass! 🎉
